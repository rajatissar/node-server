import { COURSE_ROUTES } from './routes';

const ADMIN_ROUTES = [
  `${COURSE_ROUTES.PRECEDED_BY}${COURSE_ROUTES.GET_ALL ? `/${COURSE_ROUTES.GET_ALL}` : ''}`
];

const CLIENT_ROUTES = [
  `${COURSE_ROUTES.PRECEDED_BY}${COURSE_ROUTES.GET_ALL ? `/${COURSE_ROUTES.GET_ALL}` : ''}`
];

export { ADMIN_ROUTES, CLIENT_ROUTES };
